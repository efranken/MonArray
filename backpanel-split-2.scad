// ============================================================
// backpanel.scad, center piece (segment 1 of 3) -- the flat,
// unrotated piece over the box's center chamber. See backpanel.scad's
// panel_piece().
// ============================================================

use <backpanel.scad>

panel_piece(1);
